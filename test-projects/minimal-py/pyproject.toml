[project]
name = "minimal-py"
version = "1.0.0"
description = "Minimal Python project for testing claude-learns template"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"
